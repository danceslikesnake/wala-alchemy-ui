import React, { Component } from 'react';
import PropTypes from 'prop-types';
import { StyleSheet } from 'react-native';
import { Container, Caption, Spacer, AUI_COLORS, AUI_LAYOUT, AUI_TYPOGRAPHY } from '../../../../Elements/index';

class FeedbackChoices extends Component {
  constructor(props) {
    super(props);
  }

  render() {
    const {

    } = this.props;

    return (
      <Container>
        <Caption>ollo</Caption>
      </Container>
    );
  }
}

FeedbackChoices.defaultProps = {

};

FeedbackChoices.propTypes = {

};

const styles = StyleSheet.create({

});

export  {
  FeedbackChoices
}